---
layout: layouts/base.njk
description: "Specification Description Standard (Spec Schema) for repositories: human-readable, machine-validatable, and tool-independent."
---

<section class="hero">
  <p class="hero__eyebrow">Specification Description Standard (Draft)</p>
  <h1>Spec Schema</h1>
  <p class="hero__lead">
    A unified specification standard for the repository: human-readable, machine-validatable, and independent of any specific tool.
  </p>
  <ul class="hero__theses">
    <li>A verifiable specification standard, not just a collection of Markdown files.</li>
    <li>Supports complex repository structures, not only flat lists of documents.</li>
    <li>Does not impose a workflow and is not tied to any specific AI agent and SDD tool.</li>
  </ul>
  <p class="hero__note">
    Spec Driven Development is an approach in which development relies on the specification as the primary source of truth. Our standard makes such specifications formal and verifiable: it describes how Markdown files and their structure in the repository should be organized so they stay convenient for people and reliable for automated processing.
  </p>
  <div class="hero__actions">
    <a class="button button--primary" href="{{ '/standard/' | relurl(page.url) }}">Read standard</a>
  </div>
  <p class="hero__meta">
    Early tooling is available in
    <a href="https://github.com/anatoly-tenenev/spec-cli" target="_blank" rel="noopener noreferrer">spec-cli</a>,
    which is under active development.
  </p>
</section>

<section class="panel" id="example-spec">
  <h2>Example spec</h2>
  <figure class="code">
    <figcaption class="code__label">spec.schema.yaml</figcaption>
    <pre class="code-block"><code>{{ "docs/spec.schema.yaml" | readFile | escape }}</code></pre>
  </figure>
</section>

<section class="panel">
  <h2>Possible file structure</h2>
  <p>
    No separate file structure index is needed. This is just an example layout defined and validated by
    <code>spec.schema.yaml</code>.
  </p>
  <figure class="code">
    <figcaption class="code__label">spec/</figcaption>
    <pre class="code-block"><code>{{ "docs/example.file-structure.md" | readFile | extractFencedCode("text") | escape }}</code></pre>
  </figure>
</section>
